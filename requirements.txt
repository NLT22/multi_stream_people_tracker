# =============================================================================
# Python dependencies for multi_stream_people_tracker
# =============================================================================
# IMPORTANT: pyservicemaker is NOT listed here because it cannot be installed
# from PyPI. It must be installed from the DeepStream SDK wheel file.
# See setup_venv.sh for the correct installation command.
#
# This file intentionally covers the main host-side project workflow:
# dataset conversion, YOLO training/export, Swin ReID training/export,
# and offline evaluation.
#
# PyTorch wheels are included here for one-command venv setup. If pip installs
# a CPU-only or CUDA-incompatible wheel on a specific machine, reinstall torch
# and torchvision manually with the correct CUDA wheel, then rerun this file.
# =============================================================================

# Core config/data utilities
pyyaml>=6.0
numpy>=1.26
pandas>=2.0
scipy>=1.11
opencv-python-headless>=4.9
tqdm>=4.66

# PyTorch training stack
torch
torchvision
timm>=1.0
albumentations>=2.0

# YOLO model training + ONNX export
ultralytics>=8.0.0

# ONNX export/simplification
onnx
onnxoptimizer
onnxsim
onnxscript
Cython

# Offline tracking/evaluation
motmetrics>=1.4
trackeval>=0.1
