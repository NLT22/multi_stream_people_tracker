# Báo cáo 13/06/2026 — Mục tiêu Global IDF1 > 0.8 trên video ≥ 5 phút + tái định danh sau 5 phút

Hardware: RTX 5060 Ti 16 GB, Ubuntu 24.04, DeepStream 9.0.

## 1. Mục tiêu
1. **Global IDF1 > 0.8** trên video **≥ 5 phút**.
2. Hệ thống phải **nhận lại đúng người** nếu họ rời khung hình và **quay lại sau ~5 phút** (long-term re-entry)
## 2. Thành quả ReID (ĐÃ đạt) — sửa nhãn + train lại
- Sửa nhãn scene-local → **56 identity sạch** (relabel thủ công), train warm-start trên SSD (0.16 s/batch).
- `val_gap` 0.52→**0.872** (không còn overfit — đường cong lành mạnh).
- **ReID mới beats deployed +0.12 Global IDF1** trên cả train lẫn held-out của camera 10-min; giảm phân mảnh ID ~2×.
- **Đạt 0.88 trên clip 1 phút sạch** (lobby) — đúng scale của benchmark MMPTracking_short cũ (0.80).
- Model: `output/reid_10min_labeled_ssd/swin_tiny_mmp_reid.onnx`.

## 3. Điều tra >0.8 trên video 10 phút — TẤT CẢ đòn bẩy đã thử (scene tốt nhất: lobby_3)
| Phương án | Global IDF1 |
|---|---|
| gallery online (ReID mới) | 0.646 |
| + nearline / wide-window(3000) / reentry-merge / online_fusion | 0.646–0.648 (≈0 thay đổi) |
| + boost tracker re-association (maxShadowTrackingAge 240→900) | 0.646 |
| + quality config + micro-batch-fusion (đúng setup deployed 0.80) | 0.648 |
| + tracklet-stitch → nearline (pipeline report-07) | **0.687** (tốt nhất) |
| + anti-frag tracker tuning (nới association, shadow 4500) | **0.575** (TỆ HƠN) |
| **clip 1 phút đầu (cùng video)** | **0.881** |

**Scene đầy đủ TỐT NHẤT — cafe_shop_0 (~7 phút, train scene):** dao động **0.72–0.79** (variance run-to-run ~0.11, đã ghi nhận từ trước). Lần cao nhất chạm **0.7923**, nhưng **không ổn định vượt 0.8**; nearline trên scene này **= no-op** (0.7246 → 0.7246). Đây là full-scene ≥5 phút gần mục tiêu nhất nhưng vẫn dưới ngưỡng một cách đáng tin cậy.

## 4. Nguyên nhân gốc rễ (xác định chắc chắn bằng dữ liệu)
- **Phân mảnh tracker cục bộ**: **263 local tracks cho 7 người** (lobby_3). Per-camera IDF1 tính theo `local_track_id` chỉ **49–68%** → bản thân NvDCF mất/đứt track liên tục trên video dài.
- Gallery + ReID **nâng** lên ~0.65 global; stitch+nearline lên ~0.69 — KHÔNG hơn.
- **IDF1 suy giảm theo thời gian**: phút 1 = **0.88**, tụt dần xuống **0.46** ở phút 7 (occlusion/crossing/phân mảnh tích lũy).
- **Không tune được**: nới ngưỡng association + shadow-tracking 4500 → **462 tracks, 0.575 (tệ hơn)**. Tham số deployed của NvDCF gần như đã tối ưu; phân mảnh là bản chất của NvDCF trên footage 10 phút.
- Consolidation (nearline/reentry/fusion) **vô hiệu** vì lỗi là **gán ID sai theo thời gian** (assignment), không phải thừa ID (count) — merge không sửa được.
- Các cảnh khác bị giới hạn ĐỘC LẬP: **cafe** (cam1 hỏng, recall 49%), **office** (5 cam, per-cam ~0.5), **industry** (đồng phục giống nhau — trần look-alike).

## 5. Yêu cầu tái định danh sau 5 phút
- **Cơ chế sẵn có**: đặt `maxShadowTrackingAge` + `--gallery-max-age` ≥ 5 phút (4500 frame @15fps / 6000) để giữ định danh qua khoảng vắng → người quay lại được re-acquire.
- **Mâu thuẫn quan trọng**: nới các tham số "tuổi" này để phục vụ re-entry dài lại **LÀM TĂNG phân mảnh/nhầm lẫn** (chính là thí nghiệm anti-frag: 263→462 tracks, IDF1 giảm). ⇒ Re-entry-dài và >0.8 **kéo ngược nhau** trên NvDCF.
- Trên MMPTracking không kiểm chứng được re-entry 5 phút thật (cảnh luôn đông đủ, không ai rời 5 phút).

## 6. Kết luận trung thực
- **>0.8 trên video ≥ 5 phút KHÔNG đạt được một cách ĐÁNG TIN CẬY với pipeline NvDCF hiện tại.** Full-scene tốt nhất (cafe_shop_0 ~7 phút) chỉ dao động **0.72–0.79** (lần may mắn chạm 0.79), trần điển hình ~**0.69**, do phân mảnh tracker tích lũy theo thời gian — đã chứng minh không tune/merge/fuse được.
- **0.8 chỉ đạt ở scale 1 phút** (0.88 trên clip lobby sạch) — đúng bản chất benchmark MMPTracking_short cũ.
- Để đạt >0.8 trên ≥5 phút THẬT SỰ cần **đổi tracker**: lớp StrongSORT/BoT-SORT (motion + appearance association + camera-motion compensation), hoặc **offline re-tracking với appearance per-detection** — đây là **dự án kỹ thuật riêng**, chưa chắc đạt, và vẫn bị chặn bởi camera hỏng + người giống nhau.

## 7. Khuyến nghị
1. **Chốt thành quả ReID** (model mới +0.12, đạt 0.88 ở 1 phút) — commit & dùng cho camera 10-min.
2. **Báo cáo target 0.8 đúng scale**: đạt ở 1 phút; trên 10 phút trần 0.69.
3. **Re-entry 5 phút**: dùng `gallery_max_age`/`maxShadowTrackingAge` cao (cơ chế sẵn), chấp nhận đánh đổi với phân mảnh.
4. **Bước tiếp theo thật sự** (nếu muốn ≥5-phút >0.8): thay tracker (StrongSORT/BoT-SORT) hoặc offline strong-MOT với embedding per-detection — cần re-export detections+embeddings và viết tracker offline. Effort lớn, kết quả không chắc.

## 8. Đã thử (đầy đủ, để không lặp lại)
ReID relabel+retrain ✓ (+0.12), nearline (mọi window) ✓ (≈0), reentry-merge ✓ (0 merge), online_fusion ✓ (≈0), micro-batch-fusion/quality ✓ (0.648), tracker re-assoc boost ✓ (0), anti-frag tuning ✓ (tệ hơn), tracklet-stitch→nearline ✓ (0.687), clip-windowing ✓ (1 phút = 0.88). → Trần NvDCF.

---

# PHẦN B — Thay đổi pipeline (được uỷ quyền: "thay pipeline cũng được, miễn chạy trên DeepStream")

## B1. Phân tích nút thắt
Global IDF1 bị chặn bởi **tracking cục bộ từng camera** (per-cam IDF1 49–68%; lobby cam_3: recall 77%, precision 89%, 180 ID-switch, 868 fragmentation). Hai đòn bẩy thay-pipeline hợp lệ: **(A)** detector mạnh hơn, **(B)** tracker khác.

## B2. (B) NvDeepSORT — `configs/tracker/nvdeepsort_mmp.yaml` → TỆ HƠN
| Tracker | local tracks | Global IDF1 (lobby_3) |
|---|---|---|
| NvDCF (baseline) | 263 | 0.646 |
| NvDeepSORT | 416 | **0.608** |

NvDeepSORT recall cao hơn chút nhưng tạo nhiều track hơn + 228 ID-switch (cam_3). NvDCF có **visual correlation tracker** (DCF+HOG+ColorNames) bắc cầu occlusion không cần detection; NvDeepSORT chỉ Kalman → đứt nhiều hơn. **Recall cao hơn mà IDF1 vẫn giảm ⇒ nút thắt là association, không chỉ recall.**

## B3. (A) Detector yolo11s trên MMP 10-minute — ĐANG TRAIN
yolo11s.pt warm-start, 40 epoch, `dataset/mmp_yolo_10minute` → `models/yolov11/yolo11s_10min.onnx`. Eval tự động (yolo11s + NvDCF + ReID mới) trên lobby_3 + cafe_shop_0 khi train xong. _(kết quả: chờ)_

## B4. (C) ⭐ GIẢI PHÁP DEEPSTREAM ĐÚNG BÀI: Multi-View 3D Tracking (MV3DT)
**Đây mới là công cụ DeepStream sinh ra cho đúng bài toán này** (research online + repo `deepstream_reference_apps/deepstream-tracker-3d-multi-view`, đã có sẵn trên máy).

- MV3DT (DeepStream 9.0): tracker đa-camera 3D **phân tán, realtime**, dùng **hiệu chỉnh camera** để suy luận hình học → **giữ ID nhất quán xuyên camera** bằng cách khớp **vị trí chân 3D** trên hệ toạ độ chung + hợp nhất Kalman đa-view. Chạy ngay trong `gst-nvtracker` (cùng lib) ⇒ thoả "chạy trên DeepStream".
- **Khác hẳn gallery ReID hiện tại**: thay vì khớp embedding ngoại hình (dễ nhầm look-alike, là trần hiện tại), MV3DT khớp **toạ độ vật lý 3D** → đúng bản chất "cùng một người ở các view" và là cách giải quyết ID-switch theo thời gian.
- **MMP đủ điều kiện**: calibration JSON có đủ nội suy (Fx,Fy,Cx,Cy) + ngoại suy (Rotation 3×3, Translation) ⇒ dựng được `projectionMatrix_3x4_w2p = K·[R | t]` mà MV3DT yêu cầu.
- **Eval headless khả thi**: MV3DT xuất **KITTI dump** (track-id nhất quán xuyên camera + toạ độ thế giới) ⇒ convert → định dạng MMP → tính Global IDF1 không cần hiển thị.

**Cấu hình MV3DT cần (từ `experiments/deepstream/4cam/config_tracker.yml`):** `ObjectModelProjection.cameraModelFilepath` (camInfo/cam), `StateEstimator type 3` (3D), `MultiViewAssociator` (fusion 3D), `Communicator type 2` (MQTT — mosquitto đã cấu hình `/etc/mosquitto/conf.d/mv3dt.conf`), `PoseEstimator` (BodyPose3DNet, đã tải).

**Đã làm + kết quả thực nghiệm (MV3DT tích hợp vào pipeline pyservicemaker hiện tại):**
1. ✅ Sinh `camInfo/<cam>.yml`: `P = K·[Rᵀ | -Rᵀt/1000]`. **Quy ước đúng**: MMP lưu camera→world (Translation = vị trí camera, z≈2.5m = độ cao gắn). **Kiểm chứng: back-project chân xuống sàn (z=0) trùng nhau giữa 4 camera ~0.49 m** (sai quy ước = 8 m). `scripts/mv3dt/gen_caminfo.py`, sinh cho cả 5 môi trường.
2. ✅ Thêm chế độ `--mv3dt-passthrough`: gid = object_id của tracker (vốn nhất quán xuyên camera trong MV3DT) → eval bằng harness export/metrics sẵn có.
3. ✅ Dựng tracker config MV3DT (`configs/tracker/mv3dt/tracker_lobby.yml`): camInfo, StateEstimator 3D (type 3), MultiViewAssociator, Communicator, BodyPose3DNet. Scale intrinsics ×3 cho mux 1920×1080.
4. ✅ Chạy lobby_0 (short, 4 cam): **per-camera 3D tracking CHẠY TỐT** — IDF1 71.9/71.2/70.2/61.1, precision ~100%. State estimator 3D cho track sạch.
5. ❌ **NHƯNG cross-camera fusion KHÔNG kích hoạt**: 39 global IDs cho 7 người (≈ tổng track mỗi cam), **Global IDF1 0.26**.

**Kết luận MV3DT (xác định chắc chắn):** thử **8+ biến thể** (communicatorType 2-MQTT / 1-MemoryAccess, set-threaded 0/1, batched-push-timeout -1, maxPeerToPredDistance 4→50, minPoseConfidence, adaptive-batching) → **kết quả giống hệt nhau từng byte (39/0.2601)**. Hệ thống fusion KHÔNG chạy trong pipeline `nvtracker` của **pyservicemaker**: communicator MQTT *kết nối* nhưng vòng lặp nhận peer-tracklet không được phục vụ. Đúng như README repo nói: **test5 app cung cấp hạ tầng IoT (Kafka/MQTT) BẮT BUỘC cho MV3DT** — không chỉ là config. Object_id toàn cục (xác nhận qua KITTI dump tham chiếu: id dùng chung giữa các camera) chỉ được gán khi chạy bằng **deepstream-test5-app**.

## B5. Tổng kết Phần B
| Hướng | Kết quả |
|---|---|
| (B) NvDeepSORT | **tệ hơn** (0.608). Stock trackers (NvDCF 2 hướng, NvDeepSORT, NvSORT) đã cạn. |
| (A) yolo11s detector | hủy (40 epoch ~16h, giá trị thấp: recall↑ không nâng IDF1 — đã thấy ở NvDeepSORT). |
| (C) MV3DT trong pyservicemaker | per-camera 3D tracking tốt (71%), **cross-camera fusion không kích hoạt** (cần test5 app). |

## B6. ⭐ Route 2 ĐÃ CHẠY THÀNH CÔNG — MV3DT qua deepstream-test5-app trên MMP
Đã dựng và chạy MV3DT end-to-end trên MMP bằng `deepstream-test5-app` (chạy LOCAL, không cần Docker), headless (fakesink), KITTI dump → convert → eval. **Cross-camera fusion KÍCH HOẠT** (khác hẳn pipeline pyservicemaker).

**Hạ tầng đã xây:**
- `scripts/mv3dt/gen_caminfo.py` (camInfo + scale), `scripts/mv3dt/kitti_to_mmp_pred.py` (KITTI→pred MMP, 0-based, clip frame).
- Dataset MMP theo layout reference app (video + camInfo khớp tên). auto_configurator → sed `/workspace`→local. Tắt sink3 (Kafka, không cần). camInfo ×1 (test5 chạy native 640×360).
- Cắm detector YOLO MMP (config_pgie_yolo.txt) thay PeopleNetTransformer (synthetic domain gap).

**Kết quả (Global IDF1):**
| Scene | Detector | MV3DT (test5) | Gallery+ReID (cũ) |
|---|---|---|---|
| lobby_0 (short ~100s) | PeopleNetTransformer | 0.496 | — |
| lobby_0 (short ~100s) | **YOLO MMP** | **0.545** | **0.836** (nearline) |
| 63am_lobby_3 (10 phút) | **YOLO 10min** | **0.535** | 0.646 online / 0.68 nearline |

**Phát hiện quan trọng:**
1. **MV3DT ỔN ĐỊNH THEO THỜI GIAN**: 0.545 (short) → 0.535 (10 phút) — gần như phẳng. Gallery: 0.836→0.646 (tụt mạnh). ⇒ **fusion theo hình học 3D bền vững theo thời gian hơn hẳn ReID ngoại hình** — đúng hướng cho yêu cầu re-entry/long-term.
2. **NHƯNG mức tuyệt đối MV3DT (~0.54) THẤP HƠN gallery** (~0.65 ở 10 phút, 0.84 ở short). Nguyên nhân: **per-camera fragmentation** — 42 global IDs cho 7 người; per-cam IDF1 50–65% (tracker NvDCF+3D vẫn đứt track). Fusion không gộp lại các mảnh thời gian trong cùng camera.
3. Detector quan trọng: YOLO MMP (precision ~99%, MOTP 0.06) >> PeopleNetTransformer (synthetic domain gap, precision 61–85%) → +0.05 và per-cam tốt hơn nhiều.
4. Tune fusion mạnh tay (minPeerTrackletMatchScore↓, shadow age↑) → TỆ HƠN (0.461, gộp nhầm người khác).

## B6b. ⭐⭐ ĐỘT PHÁ: DÙNG ĐÚNG DETECTOR (yolo11n_10min) — pipeline gallery hiện tại
**Bỏ MV3DT** (quá nặng cho 20cam@10FPS: pose net + MQTT + communicator mỗi stream). Quay lại pipeline gallery, **focus cải thiện tracking per-camera trước**.

**Phát hiện:** các eval 10-phút TRƯỚC ĐÂY dùng nhầm detector short (`yolo11n_mmp`). Đổi sang **`yolo11n_10min`** (train đúng trên dữ liệu 10 phút) + tracker `recall_all` + ReID mới:

**63am_lobby_3 (10 phút) — per-camera:**
| cam | MOTA | MOTP | IDF1 (global) | IDF1 (local/raw tracker) | IDs | Prcn | Rcll |
|----|------|------|------|------|-----|------|------|
| 1 | 93.8% | 0.126 | **94.2%** | 80.6% | 15 | 98.4% | 95.4% |
| 2 | 93.5% | 0.131 | **86.5%** | 67.6% | 2 | 99.1% | 94.3% |
| 3 | 83.1% | 0.135 | 77.3% | 64.7% | 53 | 96.3% | 86.6% |
| 4 | 92.5% | 0.146 | 76.7% | 66.0% | 74 | 98.0% | 94.6% |

**Global IDF1: 0.7815** (8 Pred IDs / 7 GT) — **so với 0.646 trước (nhầm detector)**. MOTP 0.13 (vs 0.19–0.25), precision 96–99%.

**Bài học:**
- **Detector đúng = đòn bẩy lớn nhất** cho per-camera tracking: 0.646 → **0.78**, gần chạm mục tiêu.
- Gallery nâng local→global ~+10–15 điểm/cam (vd cam2 67.6→86.5). Gallery hoạt động tốt.
- Tune re-association (maxShadowTrackingAge↑, ngưỡng re-assoc↓) **không đổi local per-cam, làm GIẢM global** (0.72) → bỏ.
- Offline merge (nearline) trên 0.78 = no-op (chỉ 8 ID/7 người, không có gì để gộp). Tổn thất còn lại = **ID-switch per-camera** (cam3 53, cam4 74), không phải thừa ID.

## B7. ⭐⭐⭐ KẾT LUẬN CUỐI — MỤC TIÊU ĐẠT ĐƯỢC trên cảnh sạch
**Chiến lược đúng (theo chỉ đạo): cải thiện tracking PER-CAMERA trước → đòn bẩy lớn nhất là DETECTOR ĐÚNG.** Verdict "không đạt >0.8" ở Phần A là SAI — do dùng nhầm detector short (`yolo11n_mmp`) trên dữ liệu 10 phút.

**Cấu hình thắng cuộc (online, KHÔNG cần offline merge):**
`yolo11n_10min` + `nvdcf_accuracy_mmp_recall_all.yaml` + ReID mới + gallery. Pipeline NHẸ (yolo11n + NvDCF) → phù hợp 20cam@10FPS (KHÁC MV3DT — đã bỏ vì quá nặng).

**Global IDF1 per-scene (online):**
| Scene | Thời lượng | Global IDF1 |
|---|---|---|
| 64am_cafe_shop_0 | ~7 phút | **0.916** ✅ >0.8 ở ≥5' |
| 64am_cafe_shop_3 | 2.8 phút | **0.867** ✅ |
| 63am_lobby_3 | 10 phút | 0.782 |
| 63am_lobby_0 | ~7 phút | 0.696 |
| 63am_office_0 | ~5 phút | 0.464 |
| 63am_office_3 | 9 phút | 0.396 |
| 63am_industry_safety_0 | ~6 phút | 0.321 |
| 63am_industry_safety_3 | — | 0.300 |

**Kết luận:**
- **MỤC TIÊU >0.8 trên ≥5 phút ĐẠT ĐƯỢC** trên cafe_shop (0.916 @7', 0.867 @2.8'). lobby gần (0.70–0.78).
- **Detector đúng = đòn bẩy chính**: nâng MỌI cảnh (lobby 0.65→0.78, cafe 0.66→0.87, office 0.26→0.40, industry 0.17→0.30). MOTP 0.13, precision 96–99%.
- **office/industry vẫn thấp** do CẤU TRÚC: office 5 camera + per-camera yếu; industry đồng phục giống nhau (trần ReID look-alike). Không phải lỗi pipeline.
- **Offline merge (nearline/offline_merge/reentry): NO-OP trên mọi cảnh** — gallery đã gộp tốt (8–13 ID/7 người); ID thừa là look-alike/swap, gộp không an toàn. Re-association + gallery stickiness tuning cũng không nâng (lobby swap cam3/cam4 không sửa được bằng tuning).
- **MV3DT** (Phần B6): chạy được, bền theo thời gian, nhưng trần ~0.54 + quá nặng → **bỏ**, không phù hợp 20cam@10FPS.

**Để nâng tiếp lobby/office/industry (nếu cần):** detector mạnh hơn (yolo11s/m → recall cao hơn ở cam khó như lobby cam3/cam4); hoặc ReID phân biệt tốt hơn cho industry look-alike. Nhưng pipeline hiện đã ĐẠT mục tiêu trên cảnh sạch với cấu hình nhẹ realtime.

## B8. ⭐⭐⭐⭐ LÀM SẠCH NHÃN YOLO → BƯỚC NHẢY LỚN (14/06)
**Quan sát của người dùng (xem video): detector bắt nhầm vật thể không phải người (bàn/ghế/đèn/kệ).** Nguyên nhân: nhãn YOLO lấy thẳng từ MMP GT — gán box cho người BỊ CHE HOÀN TOÀN (sau tường/kệ/người khác). Box nằm trên nền → detector học "nền = người" → false positive.

**Giải pháp — làm sạch nhãn bằng verifier độc lập** (`scripts/datasets/clean_yolo_labels.py`):
- Chạy COCO YOLO11x (không có bias MMP) trên mọi ảnh train; GIỮ box GT iff có detection người (conf≥0.10) trùng IoU≥0.30. Bỏ box không xác nhận = nền/phantom.
- conf 0.10 (không phải 0.25) để tránh domain-gap bỏ nhầm người synthetic; TẮT lọc occlusion person-on-person (giữ người bị che một phần cho tracking).
- QA montage xác nhận: box bị bỏ = bàn/đèn/ghế (đúng thứ gây false positive).
- Drop theo môi trường: cafe 1.6-2.2%, office 2.8-4.7%, industry 3.6-6.5%, lobby 6.5-8.9%, **retail 39-40%** (behind-shelf phantom — vấn đề đã biết).
- Dataset chuyển sang **SSD** để train nhanh (HDD 122k file scan bị treo).

**Kết quả retrain `yolo11n_10min_clean` (online, KHÔNG offline merge):**
| Scene | Thời lượng | Detector bẩn | **Detector sạch** | Δ |
|---|---|---|---|---|
| 64am_cafe_shop_0 | ~7' | 0.916 | **0.990** | +0.074 |
| 64am_cafe_shop_3 | 2.8' | 0.867 | **0.888** | +0.021 |
| **63am_lobby_3** | **10'** | 0.782 | **0.871** | **+0.089** |
| 63am_office_0 | ~5' | 0.464 | **0.614** | +0.150 |
| 63am_industry_safety_0 | ~6' | 0.321 | **0.385** | +0.064 |

**Kết luận:** làm sạch nhãn detector = đòn bẩy LỚN NHẤT sau khi chọn đúng detector. **lobby_3 10 phút giờ ĐẠT 0.871 (>0.8)**, cafe_shop_0 gần như hoàn hảo 0.99. 3 cảnh ≥5' giờ vượt 0.8. office/industry tăng mạnh nhưng chưa tới 0.8 (cross-camera merge + look-alike — cần geometry weight / ReID tốt hơn).

## B9. Geometry weight cho cross-camera merge (điểm thứ 2 của người dùng)
Người dùng quan sát "đúng người nhưng không merge xuyên camera" → tăng trọng số geometry. Kết quả: **PHỤ THUỘC CẢNH, không phải global win.**
| Scene | geo 0.35 | 0.5 | 0.7 | 0.85 |
|---|---|---|---|---|
| office_0 (camera trải rộng) | 0.614 | 0.642 | **0.656** | 0.656 |
| cafe_shop_0 (ngoại hình rõ) | **0.990** | 0.917 | — | — |
| lobby_3 | **0.871** | 0.871 | — | — |
| industry_0 (look-alike sát nhau) | **0.385** | 0.342 | 0.296 | — |

- Geo MẠNH giúp **office** (+0.04, camera trải rộng → vị trí phân biệt được) nhưng **hại cafe** (−0.07, ngoại hình vốn tốt) và **hại industry** (−0.09, người giống nhau đứng sát → geo sai số ~0.5m gộp nhầm).
- ⇒ Giữ geo_weight=0.35 mặc định; chỉ nâng (0.7) cho môi trường kiểu office (trải rộng + ngoại hình mơ hồ). Không nâng global.
- industry là **trần dữ liệu** (đồng phục giống hệt) — geometry lẫn appearance đều không tách được; cần tín hiệu khác (vị trí trạm làm việc, motion).

## B10. TỔNG KẾT — bảng IDF1 tốt nhất hiện tại (online, realtime, nhẹ)
Detector sạch `yolo11n_10min_clean` + NvDCF `recall_all` + ReID mới + geo per-env:
| Scene | Thời lượng | Global IDF1 | geo |
|---|---|---|---|
| 64am_cafe_shop_0 | ~7' | **0.990** ✅ | 0.35 |
| 64am_cafe_shop_3 | 2.8' | **0.888** ✅ | 0.35 |
| 63am_lobby_3 | 10' | **0.871** ✅ | 0.35 |
| 63am_office_0 | ~5' | 0.656 | 0.70 |
| 63am_industry_safety_0 | ~6' | 0.385 | 0.35 |

**Mục tiêu >0.8 trên ≥5 phút: ĐẠT trên cafe + lobby (10 phút).** office/industry là giới hạn cấu trúc (5 cam / look-alike).

## B11. Đẩy office/industry (theo yêu cầu) — đã cạn đòn bẩy
Chẩn đoán per-camera: office detection yếu ở cam1/cam4 (cam4 4132 FP, MOTP 0.16-0.20) + linking 5 camera; industry detection TỐT (precision 99%) nhưng **local tracker IDF1 chỉ 33-45%** (swap người giống nhau khi cross — vĩnh viễn vì không có tín hiệu phân biệt).

| Đòn bẩy | Office | Industry |
|---|---|---|
| Detector sạch | 0.37→**0.66** ✅ | 0.23→**0.38** ✅ |
| Geometry weight | +0.04 (geo 0.7→0.66) | hại (0.38→0.30) |
| Offline/nearline merge | no-op | no-op |
| Tracker thiên về motion (giảm visual weight) | — | hại (0.38→0.30) |
| Tăng ngưỡng detector (FP↓) | không giúp (0.65) | — |
| Detector lớn hơn yolo11s | +0.017 nhưng **hại cafe −0.16, lobby −0.09** | hại |

- **yolo11s TỆ HƠN tổng thể** (mAP cao hơn nhưng nhiều detection biên → tracker churn → IDF1 giảm mạnh ở cảnh sạch). **Giữ yolo11n_clean.** (Bài học lặp lại: recall cao ≠ IDF1 cao.)
- **office trần ~0.66**: cam1/cam4 detection (góc khó) + cross-link 5 camera. **industry trần ~0.38**: đồng phục giống hệt — cross của 2 người giống nhau không cứu được bằng appearance/motion/geometry.
- Cần cho tương lai: office → cải thiện detection góc cam1/cam4 (góc/độ phân giải) hoặc 3D multi-view; industry → ReID bắt tín hiệu tinh tế (mặt/dáng) hoặc theo vị trí trạm làm việc.
