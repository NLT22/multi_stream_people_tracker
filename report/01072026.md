# Báo cáo ngày 01/07/2026
## Tầng RAG/Ask: hỗ trợ OpenAI gpt-4o + Anthropic, nạp khóa qua `.env`

Tiếp nối [29/06](29062026.md). Hôm nay chỉ làm về tầng RAG/Ask.

## Đã làm
- Thêm nhà cung cấp **OpenAI gpt-4o** bên cạnh Anthropic (`src/rag/agent.py`); chọn theo tên mô hình qua
  biến `RAG_MODEL`, không cần sửa mã.
- Nạp khóa API qua **`.env`** (gitignore, kèm mẫu `.env.example`).
- Khai báo phụ thuộc còn thiếu: `scikit-learn`, `openai`.

## Kết quả
- `pytest tests/test_rag.py` → **12/12 pass**.
- Chạy thật gpt-4o trên `64pm_cafe_shop_1`: hỏi "which area got the most attention?" → agent gọi
  `top_zones(occupancy)` → trả lời đúng **`cam1:TABLE` (2386,67 người-giây)**.
- Commit: `e54da8d` (scikit-learn), `2eba8d4` (gpt-4o), `ecc57a0` (`.env`).

## Còn lại
- Text-to-SQL dự phòng cho câu hỏi mở.
- Tìm kiếm theo ảnh kém nhất ở retail (IDF1 0,661).
- Hiện chạy trên dữ liệu đã ghi, chưa trực tuyến.
