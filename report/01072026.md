# Báo cáo ngày 01/07/2026
## Hoàn thiện tầng hỏi–đáp RAG/Ask: chạy được cả OpenAI gpt-4o lẫn Anthropic Claude, nạp khóa qua `.env`, và kiểm thử thực tế trên cảnh `64pm_cafe_shop_1`

**Phần cứng:** GPU NVIDIA RTX 5060 Ti (16 GB), Ubuntu 24.04, DeepStream 9.0, CUDA 13.1, TensorRT 10.14.
Tiếp nối [29/06](29062026.md) (khung RAG `src/rag/` + view "Ask"). Hôm nay **chỉ làm về tầng RAG/Ask**.

**Tóm tắt:** tầng hỏi–đáp nay hỗ trợ **hai nhà cung cấp LLM** (chọn theo `RAG_MODEL`), nạp khóa từ `.env`,
đã **chạy thật với gpt-4o** trên cảnh tốt nhất và cho câu trả lời đúng. 12/12 unit-test vẫn pass.

---

## 1. Đã làm

- **Thêm nhà cung cấp OpenAI (gpt-4o)** bên cạnh Anthropic trong `src/rag/agent.py`. Dùng chung lược đồ
  công cụ (`TOOLS`) và bộ điều phối (`dispatch`); chỉ tách vòng lặp gọi LLM. Chọn nhà cung cấp **theo tên
  mô hình** (`gpt-*`/`o*` → OpenAI, `claude-*` → Anthropic), lấy từ biến `RAG_MODEL` (mặc định
  `claude-sonnet-4-6`) — đổi mô hình **không cần sửa mã**.
- **Nạp khóa API qua `.env`**: agent tự đọc `.env` ở gốc dự án (không thêm phụ thuộc, không ghi đè biến
  môi trường sẵn có). `.env` được gitignore; thêm mẫu `.env.example` (có commit).
- **Vá phụ thuộc thiếu** trong `requirements.txt` (chạy được từ bản clone mới): `scikit-learn` (dùng bởi
  `src/eval/offline_anchor_faithful.py` cho `live_buffered`) và `openai`.
- Cập nhật `/ask` (`src/rag/api.py`) báo đúng khóa còn thiếu theo từng nhà cung cấp.

## 2. Đã tạo ra

| Sản phẩm | Vị trí |
|---|---|
| Agent tool-use đa nhà cung cấp (OpenAI + Anthropic) | `src/rag/agent.py` |
| Cấu hình khóa qua `.env` + mẫu | `.env` (gitignore), `.env.example` |
| 6 hàm truy vấn chi tiết = 6 endpoint = 6 công cụ LLM | `src/rag/queries.py`, `src/rag/api.py` |

6 hàm chi tiết (đã có, nay gọi được từ cả hai LLM): `top_zones`, `zone_occupancy`, `person_timeline`,
`person_dwell`, `person_trajectory_bev`, `search_person_by_image`.

## 3. Kết quả

**Unit-test (cổng đúng đắn, không cần khóa/mạng):**
```
pytest tests/test_rag.py  →  12 passed in 2.5s
```

**Chạy thật với gpt-4o** trên `64pm_cafe_shop_1` (store dựng bằng `src.rag.ingest`; 10 người, 142 985
phát hiện, dài ~355 giây):

| Câu hỏi | Công cụ agent gọi | Kết quả trả về |
|---|---|---|
| "which area got the most attention?" | `top_zones(metric=occupancy, k=1)` | `cam1:TABLE` — **2386,67 người-giây** |

Câu trả lời đúng: `cam1:TABLE` là khu vực được chú ý nhất. *(occupancy tính theo **người-giây** =
tổng thời gian hiện diện cộng dồn; 2386,67 / 355 ≈ 6,7 người trung bình trong vùng bàn — hợp lý với cảnh
quán cà phê.)*

**Commit hôm nay:** `e54da8d` (khai báo scikit-learn), `2eba8d4` (nhà cung cấp OpenAI gpt-4o),
`ecc57a0` (hỗ trợ `.env`).

## 4. Còn lại

- Text-to-SQL dự phòng cho câu hỏi mở (rào chắn chỉ-đọc).
- Tìm kiếm theo ảnh kém tin cậy nhất ở **retail** (IDF1 0,661, che khuất bởi kệ).
- Cần **đặt tên vùng theo môi trường** trong trình ROI để trả lời "kệ nào"; MMP không có thời gian thực
  nên đặt qua `--epoch-iso`.
- Hiện chạy trên lần chạy **đã ghi (batch)**, chưa **trực tuyến**.

**Cho thầy:** mặc định dùng **gpt-4o** hay **Claude** (đã hỗ trợ cả hai)? Demo trên dữ liệu đã ghi hay
bắt buộc trực tuyến?
